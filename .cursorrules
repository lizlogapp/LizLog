# 安全與權限規則

- 禁止執行任何會修改系統環境變數 (setx, export) 的指令。
- 禁止執行任何涉及路徑 `C:\Windows`, `C:\Users` (除目前專案外) 的操作。
- 嚴禁執行任何刪除指令 (如 rm, rd, del, git clean)，除非我明確要求並指定檔案。
- 所有操作必須侷限在目前專案資料夾內：{{project_path}}。
- 在建議任何系統級別的變更前，必須先解釋該指令的潛在影響。
